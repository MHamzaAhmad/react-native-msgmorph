/**
 * MsgMorph React Native SDK
 * 
 * Native feedback widget and live chat for React Native apps.
 */

// Main exports will be added during implementation
export const VERSION = '0.1.0';

// Placeholder for SDK initialization
export function init(config: { widgetId: string; apiBaseUrl?: string }) {
  console.log('MsgMorph SDK initialized with:', config);
}
